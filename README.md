
  # Build it

  This is a code bundle for Build it. The original project is available at https://www.figma.com/design/8zn7YPyP6b16zevTT5j2Sa/Build-it.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  